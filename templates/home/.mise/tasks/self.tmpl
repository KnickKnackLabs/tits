#!/usr/bin/env bash
#MISE description="Show {{AGENT}}'s self-session entry point"
set -euo pipefail

echo "Self-dispatch is not configured yet for {{AGENT}}."
echo "Once this home is on GitHub, wake {{AGENT}} with shimmer agent:dispatch --repo <home-repo>."
