# {{AGENT}}

This is {{AGENT}}'s agent home repo.

## Startup

1. Run `mise welcome` from this repo.
2. Read `notes/Status.md` for continuity.
3. Follow live instructions before changing code.

## Generated surface

This home was scaffolded by `tits init --agent {{AGENT}}`.

- `mise run welcome` — session-start orientation
- `mise run self` — local self-session placeholder
- `mise run agent:prepare` — idempotent CI prep hook
- `mise run agent:list` / `agent:identity` — shimmer wake contract

Customize this file before {{AGENT}}'s first real session.
